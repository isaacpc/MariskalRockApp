package com.isaacpc.mariskalrock.activity;

import android.app.Activity;

/**
 * Activity que muestra el detalle de una portada para verla con zoom.
 * 
 * @author Isaac Palomino
 * 
 */
public class PortadaDetalleActivity extends Activity {

        //    private AQuery aq;
        //
        //    @Override
        //    protected void onCreate(Bundle savedInstanceState) {
        //	super.onCreate(savedInstanceState);
        //
        //	setContentView(R.layout.portada_detalle);
        //
        //	// Mostramos los parámetros recibidos de la actividad mainActivity
        //	Bundle reicieveParams = getIntent().getExtras();
        //	String url = reicieveParams.getString("url");
        //
        //	if (!StringUtils.isEmpty(url)) {
        //	    aq = MariskalRockApplication.getAQueryInstance();
        //	    aq.id(R.id.webView1).progress(R.id.progressBarDetalle).webImage(url);
        //	}
        //    }
}